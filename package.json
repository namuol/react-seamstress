{
  "name": "react-declarative-styles",
  "version": "0.0.0",
  "description": "Experimental interface for exposing style-related state inside complex React components",
  "main": "index.js",
  "scripts": {
    "test": "babel-node test | tap-difflet",
    "hack": "nodemon -w src -w test -w package.json --exec npm test & npm run example",
    "example": "cd example && npm install && npm start",
    "start": "npm run hack"
  },
  "author": "Louis Acresti <louis.acresti@gmail.com> (http://namuol.github.io)",
  "license": "MIT",
  "devDependencies": {
    "babel": "^5.8.21",
    "nodemon": "^1.4.1",
    "tap": "^1.3.2",
    "tap-difflet": "^0.3.0",
    "tape": "^4.2.0"
  },
  "dependencies": {}
}
